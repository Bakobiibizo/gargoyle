                                                                                                                                                                                                              End-to-End Testing Instructions                                                                                                                                                                                           
                                                                                                                                                                                                                            
  1. Build & Launch

  cd D:\apps\gargoyle
  npm run tauri dev

  This compiles the Rust backend and launches the Tauri window with the React frontend.

  2. Verify Each Tab

  Entities tab:
  x- Click "+ Create Entity" — pick a type (e.g., note), enter a title, set status to draft, click Create
  x- Verify the entity appears in the list with the correct type badge and status badge
  x- Click the entity row to expand details — verify ID, timestamps, canonical fields
  x- Create a few more entities of different types (metric, task, project, decision)
  x- Use the type filter dropdown — verify it filters correctly
  x- Use the status filter — verify it works
  x- Click Delete on an entity, confirm in the modal — verify it disappears (soft delete)

  Templates tab:
  x- Verify the template dropdown loads all 23 templates
  x- Select analytics-metric-tree — it should show no prerequisites (or all satisfied)
  x- Fill in params (metric_name: "Revenue", data_source: "Stripe") and click Run
  x- Verify output shows: run ID, applied operations with entity IDs
  x- Select analytics-experiment-plan — it should show prerequisite satisfied (metric exists now)
  o- Run it with params — verify it creates experiment + relation entities
  x- Check run history at the bottom

  Graph tab:
  x- Select an entity from the dropdown (pick one created by a template run)
  x- Set depth to 2, click Load
  x- Verify the SVG graph renders with colored nodes and labeled edges
  x- Hover nodes to see tooltips
  x- Click a node to re-center the graph on that entity
  x- Try dragging a node

  Search tab (and header search bar):
  o- Type a title fragment in the search bar — verify dropdown appears with matching entities
  o- Click a result — verify it navigates to the Graph tab centered on that entity
  o- Use the dedicated Search tab for the same functionality

  Dedup tab:
  x- Create two entities with identical titles (e.g., two notes both titled "Q1 Review")
  o- The dedup pipeline should detect the duplicate
  o- Check the Dedup tab for a suggestion
  na- Review the suggestion, click Accept, verify the merge confirmation modal
  na- Check the Claims sub-tab for any claims generated by template runs

  Workflows tab:
  x- Verify the dependency graph SVG renders showing template nodes grouped by category
  x- Check which templates show as "completed" (green) based on your runs
  x- Switch to Chain View — verify dependency chains appear
  x- Switch to Run Timeline — verify your template runs appear with timestamps

  Chat tab:
  o- Verify the status bar shows LLM connection status (green dot if connected, red if not)
  o- Type a message and press Enter — verify the assistant responds
  o- Verify Shift+Enter inserts a new line without sending
  o- Click "System prompt" toggle — verify the system prompt textarea appears
  o- Modify the system prompt and send a message — verify the response reflects the new prompt
  o- Click "Clear" — verify the conversation is cleared
  o- Verify model name and token count appear below assistant messages

  3. Cross-Cutting Checks

  x- Data persistence: Close and reopen the app — all entities, relations, and runs should persist
  o- Error handling: Try creating an entity with an invalid status (e.g., status "bogus" for a metric) — should show a validation error
  o- Backward status transition: Update an entity's status backward without a reason — should fail. Add a reason — should succeed
  o- Template prerequisites: Try running analytics-experiment-plan on a fresh DB with no metrics — should warn about unmet prerequisites (unless you check "Force run")

  4. Run All Automated Tests

  # Rust unit + integration tests (~555 total)
  cd src-tauri && cargo test

  # Extended stress tests with fuzz
  cargo test --test stress_extended -- --nocapture

  # Performance benchmarks
  cargo test --test perf_benchmarks -- --nocapture

  # TypeScript compile check
  cd .. && npx tsc --noEmit

  # Production build
  npx vite build